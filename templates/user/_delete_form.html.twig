<form method="post" action="{{ path('user_delete', {'id': user.id}) }}" onsubmit="return confirm('¿Está seguro de que quiere eliminar el usuario?');">
    <input type="hidden" name="_token" value="{{ csrf_token('delete' ~ user.id) }}">
    <button class="btn">Eliminar</button>
</form>
